services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scorecard
      POSTGRES_PASSWORD: scorecard
      POSTGRES_DB: interview_scorecard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
